{
  "name": "pottery-emaly-project",
  "version": "0.2.0",
  "private": true,
  "description": "Premium family enamelware platform — evidence-weighted Shape Hunt tools.",
  "type": "module",
  "scripts": {
    "clean": "node scripts/clean-shape-hunt.mjs",
    "test": "node --check apps/shape-hunt/src/geometry.js && node --check apps/shape-hunt/src/state.js && node --check apps/shape-hunt/src/main.js && node --check scripts/validate-shape-hunt.mjs && node --check scripts/clean-shape-hunt.mjs",
    "validate:shape-hunt": "node scripts/validate-shape-hunt.mjs",
    "doctor": "npm run clean && npm test && npm run validate:shape-hunt",
    "serve:shape-hunt": "python3 -m http.server 5173 --directory apps/shape-hunt"
  },
  "engines": {
    "node": ">=20"
  }
}
